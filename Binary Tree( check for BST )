class Solution {
  public:
    int countNonLeafNodes(Node* root) {
        // Code here
        int count1=count2=0;
        if(root==NULL) return 0;
        if(root->left==NULL && root->right==NULL) {
            count2++;
        }
        
    }
};
