#include<bits/stdc++.h>
using namespace std;
int main() {
	int tc;
	cin>>tc;
	while(tc--) {
		int n;
		cin>>n;
		int count=0;
		while(n>0) {
			n=n&(n-1);
			count++;
			n=n>>1;
		}
		cout<<count<<endl;
	}
	return 0;
}
